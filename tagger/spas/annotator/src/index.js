import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('annotator-app-root'));
root.render(
  <React.StrictMode>
    Hello World!
  </React.StrictMode>
);
